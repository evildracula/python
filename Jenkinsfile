pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh 'python --version'
                sh 'pwd'
                sh 'ls -l'
                sh 'cat README.md'
            }
        }
    }
}